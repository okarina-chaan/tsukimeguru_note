<div class="container mx-auto px-4 py-10 max-w-lg">
  <h1 class="text-3xl font-display text-center mb-8">月星座診断</h1>

  <div class="card bg-base-200 shadow-lg">
    <div class="card-body space-y-6">

      <p class="text-sm opacity-80">
        あなたの「内側の性質」を表す月星座を診断してみましょう
      </p>

      <%= form_with url: moon_sign_path, method: :post, data: { turbo: false } do |f| %>

        <div class="form-control">
          <%= f.label :birth_date, "生年月日", class: "label font-bold" %>
          <%= f.date_field :birth_date, required: true, class: "input input-bordered" %>
        </div>

        <div class="form-control">
          <%= f.label :birth_time, "出生時間（任意）", class: "label font-bold" %>
          <%= f.time_field :birth_time, class: "input input-bordered" %>
        </div>

        <div class="form-control">
          <%= f.label :prefecture, "生誕地（任意）", class: "label font-bold" %>
          <%= f.select :prefecture, Prefecture::LIST, include_blank: "選択してください", class: "select select-bordered" %>
        </div>

        <div class="pt-4">
          <%= f.submit "診断する", class: "btn btn-primary w-full" %>
        </div>

      <% end %>
    </div>
  </div>
</div>
